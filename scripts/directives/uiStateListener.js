app.directive('uiStateListener', function() {
	uiStateType: '@',
	uiStateValue: '@'
	uiStateMatch: '@'
		if (newValue = uiStateValue) {
			element.toggleClass
		}

	register: function(key, callback) {
		in callback toggle
	}

$scope.selectedPart = 